{
  "name": "veloinsights",
  "version": "1.0.0",
  "description": "VeloInsights Dashboard - Monorepo",
  "private": true,
  "scripts": {
    "dev": "cd frontend && npm run dev",
    "build": "cd frontend && npm run build",
    "install-all": "cd frontend && npm install && cd ../API && npm install"
  },
  "workspaces": [
    "frontend",
    "API"
  ]
}